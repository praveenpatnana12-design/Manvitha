# Server Configuration
PORT=5000
NODE_ENV=development

# JWT Configuration
JWT_SECRET=supersecretkeychangeinproduction
JWT_EXPIRE=24h

# Database Configuration (MySQL)
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=manvitha_billing
DB_PORT=3306

# AI Configuration
# Get your Gemini API Key from Google AI Studio
GEMINI_API_KEY=
# Or use OpenAI
OPENAI_API_KEY=

# Email SMTP Configuration (Optional, falls back to mock logger)
SMTP_HOST=smtp.mailtrap.io
SMTP_PORT=2525
SMTP_USER=
SMTP_PASS=
SMTP_FROM=billing@manvithatours.com
